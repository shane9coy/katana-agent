# Custom Slash Commands

This directory contains custom slash commands for Gemini CLI.

## Structure

Commands can be either:
- Single TOML files: `plan.toml`
- Directories with TOML files: `deploy/prod.toml`

## Available Commands

Commands are synced from `~/.katana/commands/` during initialization.
